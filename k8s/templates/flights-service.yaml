apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.flight-service.name }}
spec:
  selector:
    app: {{ .Values.flight-service.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.flight-service.image.exposedPort }}
  type: LoadBalancer